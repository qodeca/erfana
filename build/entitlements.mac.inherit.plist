<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Minimal inherited entitlements applied to helper processes
       (Renderer / GPU / Plugin). JIT is kept on because Electron's
       Renderer helper needs V8 execution; GPU/Plugin accept it too
       without granting wider privilege.
       FORBIDDEN: cs.disable-library-validation, cs.allow-dyld-environment-variables. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
</dict>
</plist>
