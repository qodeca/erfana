# Local kit patches

This file lists local changes to copied kit files that an upgrade must preserve or upstream, so a kit refresh does not silently remove project-specific operating guarantees.

- Added `.xezar/docs/briefs.md` and linked it from the kit documentation and leader-guide detail to standardize row-specific dispatch briefs for issue #175.
- Configured the repository gate's two application lanes in `.xezar/pipeline/config.json`, bounded its config read in `gate-parallel.mjs`, recorded and checked the resolved schedule at execution, kept coverage ahead of unit tests and build with fail-closed command guards, and switched lint to read-only `lint:check` for issue #169.
- Granted `mcp__chrome-devtools__emulate` to the `review` step of `.xezar/workflows/qa.yaml` and `.xezar/workflows/design-review.yaml`, and gave both steps the same explicit `bashAllowlist` as the other read-only review workflows, for issue #177. `evaluate_script` stays ungranted: the engine gives a step no hook or settings field, and it runs a read-only step with `--setting-sources user`, so a project-registered PreToolUse hook would never load there. The guard that should gate it is ready in `scripts/devtools-evaluate-guard.mjs` (purpose comment plus an in-page origin guard for the dev server or `out/renderer/index.html`); wiring it needs an upstream engine change that lets a step pass its own settings.
