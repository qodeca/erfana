# gitleaks false-positive allowlist. Each line is a finding fingerprint
# (commit:file:rule:line) gitleaks should skip. Entries here are reviewed
# non-secrets — test fixtures that resemble high-entropy tokens.
#
# The Secret Scan workflow runs `gitleaks detect --log-opts="--all"`, which
# scans every ref in the repository, not just this branch. So a finding in a
# commit that exists only on `graph` still fails the check here, and this file
# has to carry the fingerprint even though the offending file is not on
# `develop`.
#
# graph-mcp-schema.test.ts: a static 64-hex string used as a fake
# randomBytes(32) MCP bearer token in commit e5ec5f9 (issue #21). The file
# also carries an inline `gitleaks:allow` on the `graph` branch; this entry
# covers the original historical commit, which predates that comment.
e5ec5f9714543b6e9f71c39212b7349cf19f28f6:src/shared/ipc/graph-mcp-schema.test.ts:generic-api-key:440

# screenshotOverlay.test.ts: the RFC 4122 example UUID used as a fake overlay
# capture token in commit 69df015 (issue #60). A follow-up commit replaced it
# with a low-entropy literal; this entry covers the historical commit only.
69df015eb860fa966a306c684827fe89207aa1ec:src/preload/screenshotOverlay.test.ts:generic-api-key:29

# preview link tests: the same fake 32-hex `erfana-preview://` root token used
# across three suites (sd-074b). It is a test fixture, never a credential — the
# real token is minted per view by PreviewRootRegistry and never leaves the main
# process. The current lines carry an inline `gitleaks:allow`; these entries
# cover the historical commits, which gitleaks still sees because the workflow
# scans every ref with `--log-opts="--all"`.
e42a931a80ddd4a8d1185bf3b5ba4c0fb439855c:src/main/services/preview/PreviewNavigationPolicy.test.ts:generic-api-key:13
e42a931a80ddd4a8d1185bf3b5ba4c0fb439855c:src/main/services/preview/previewLinkNavigation.test.ts:generic-api-key:16
555a0732576efaa0fdc93f14e5560c81ec923e4e:src/main/services/preview/previewLinkBridge.test.ts:generic-api-key:25

# #124 HTML preview tests: fake 32-hex preview root tokens
# (`erfana-preview://<32-hex>/…`) used as fixtures in commit 8576397d. They
# are random identifiers for a local custom scheme, not credentials (see the
# PreviewRootRegistry.ts header). The lines also carry inline `gitleaks:allow`.
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/__test-helpers__/previewPageNavigatorHarness.ts:generic-api-key:21
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/previewResponseHeaders.test.ts:generic-api-key:19
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/previewViewNavigation.test.ts:generic-api-key:48
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/previewLivePage.outcome.test.ts:generic-api-key:25
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/previewLivePage.test.ts:generic-api-key:24
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/previewCsp.test.ts:generic-api-key:15
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/previewCsp.test.ts:generic-api-key:16
8576397d9aa8777a9329b7b7100fa93c54037597:src/main/services/preview/previewUrl.test.ts:generic-api-key:8
8576397d9aa8777a9329b7b7100fa93c54037597:src/shared/ipc/preview-navigation-schema.test.ts:generic-api-key:100
